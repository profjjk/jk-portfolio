import { ProjectHeader } from '../../components';

export const ReactChallenges = () => {
    return (
        <main id={'react-challenges'}>
            <ProjectHeader project={'React Challenges'} right={'310px'} />
        </main>
    )
}