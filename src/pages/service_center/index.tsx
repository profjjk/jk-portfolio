import { ProjectHeader } from '../../components';

export const ServiceCenter = () => {
    return (
        <main id={'service-center'}>
            <ProjectHeader project={'Service Center'} right={'274px'} />
        </main>
    )
}